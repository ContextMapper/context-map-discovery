apply plugin: "war"

bootWar {
    mainClassName = "org.contextmapper.generated.referencemanagementcontext.ReferenceManagementContextApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
